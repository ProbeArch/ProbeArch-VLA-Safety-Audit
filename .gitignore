# Python
__pycache__/
*.py[cod]
.venv/
.venv-audit/
venv/
*.egg-info/

# Rollout data (large; keep out of git)
rollouts/
audit/
audit-mlx/
audit-pilot/
audit-pilot-cuda/
analysis/results/
analysis/saved_rollouts/

# WSL setup scripts (machine-specific, not part of the audit artifact)
wsl-setup/

# Logs
*.log

# OS
Thumbs.db
.DS_Store

# HuggingFace cache
.cache/huggingface/

# Agent orchestration state (generated; untracked but must be ignored so the
# ship.sh dirty-tree guard only sees real changes)
.pi/
.pi-glla/
.codegraph/
.claude/
.ruff_cache/
